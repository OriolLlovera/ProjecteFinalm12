{# {% extends "BackEndBundle:Default:index.html.twig" %}
{% block page_content %}
{% if currentUser != null %}
    <div id="belousovChat" data-author="{{ currentUser.id }}" data-addressee="" data-action="{{ getMessageUrl }}" >
        <div id="belousovMessageZone"></div>
        <div id="belousovFormZone">
            {{ form_start(chatForm) }}
    
                {{ form_widget(chatForm.messageText) }}
    
                {{ form_errors(chatForm) }}
    
            {{ form_end(chatForm) }}
        </div>
    </div>
    <div id="selectUser">
        {% for user in users %}
            {% if user.id != currentUser.id %}
                <a href="" onclick="return false;" data-number="{{ user.id }}" class="changeUser" >{{ user.username }}</a>
            {% endif %}
        {% endfor %}
    </div>
{% else %}
    <p>ERROR!</p>
{% endif %}
{% endblock %} #}

{% extends "BackEndBundle:Default:index.html.twig" %}
{% block page_content %}

    {% if currentUser != null %}
      <div class="container fonschat">
        <div class="container marginbotchat ">
            <div class="row">
                <div class="col-md-5 col-md-push-3">
                    <div class="panel panel-primary margintopchat">
                        <div class="panel-heading" id="belousovChat" data-author="{{ currentUser.id }}" data-addressee="" data-action="{{ getMessageUrl }}">
                            <span class="glyphicon glyphicon-comment"></span><b> Xat</b>                    
                        </div>
                        <div class="panel-body bordechat" id="belousovMessageZone">
                        </div>
                       
                        <div class="panel-footer bordechat" class="target" id="belousovFormZone">
                            <div class="input-group">
                               {{ form_start(chatForm) }}

                               {{ form_widget(chatForm.messageText) }}

                               {{ form_errors(chatForm) }}

                               {{ form_end(chatForm) }}
                           </div>
                       </div>
                   </div>
               </div>
               <div class="col-md-2 col-md-push-3">
                    <div class="panel panel-primary margintopchat">
                        <div class="panel-heading" id="belousovChat" data-author="{{ currentUser.id }}" data-addressee="" data-action="{{ getMessageUrl }}">
                            <span class="glyphicon glyphicon-user"></span><b> Usuaris</b>
                        </div>
                        <div class="panel-body bordechat" id="belousovMessageZone">
                            <div id="selectUser">
                            {% for user in users %}
                                {% if user.id != currentUser.id %}
                                    <div class="row margintextchat">
                                    <a href="" onclick="return false;" data-number="{{ user.id }}" class="changeUser" >{{ user.username }}</a>
                                    </div>
                                {% endif %}
                            {% endfor %}
                        </div>
                        </div> 
                   </div>
               </div>
           </div>
       </div>
     </div>
   {% endif %}
{% endblock %}



                        